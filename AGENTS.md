# 项目协作约定

## 可发布版本前的存档策略

- 在项目进入可发布版本之前，不要求兼容旧存档。
- 修改数据结构、规则或内容时，可以直接更新当前存档格式，不必为旧存档编写迁移、兼容模式或保留旧规则的分支。
- 旧存档不兼容时，应明确提示需要新建旅程，不必为了继续读取旧档而限制设计与实现。
- 当前版本内的保存、读取、行动结算一致性及防止重复消耗、重复领取仍需正确实现和验证。
- 此约定优先于现有设计文档中关于旧档兼容与迁移的要求；进入可发布版本时，再明确版本兼容和迁移策略。
